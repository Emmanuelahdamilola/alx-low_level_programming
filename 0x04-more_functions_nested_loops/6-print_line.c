#include "main.h"
/**
 * print_line - draws a straight line in the terminal
 * @n: input an integer
 * Return: retrun void
*/
void print_line(int n)
{
	int i;

	if (i == 95)
	if (n > 0)
	{
		_putchar(i * n);
	}
	else
	{
		n <= 0;
		_putchar('\n');
	}
}
