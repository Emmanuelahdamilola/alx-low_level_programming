#ifndef MACROS_H
#define MACROS_H

/**
 * Description: functio-like macro ABS(X)that
 * computes the absolute value of a number x
 * @x: absolute value
 */

#define ABS(x) (((x) < 0) ? -(x) : (x))

#endif
